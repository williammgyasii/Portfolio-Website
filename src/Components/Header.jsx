import React from "react";
import Navbar from "./Navbar";

const Header = () => {
  return (
    <header className="w-full relative flex flex-col items-center justify-center">
      <Navbar />
    </header>
  );
};

export default Header;
